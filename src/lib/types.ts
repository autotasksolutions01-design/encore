// No-op barrel for API route type safety — placeholders reference Prisma client indirectly.
// When a route needs DB types: import type { Prisma } from "@prisma/client";

export type { SkillLevel, Visibility, CollabRole, ConnectionStatus, MessageStatus, ReportStatus, JamStatus, JamResponseType } from "@prisma/client";
